class Solution:
    def reverse(self, x: int) -> int:
        if x<0:
            x=x*-1
            num=int(str(x)[::-1])*-1
        else:
            num=int(str(x)[::-1])
        if -2**31<num<2**31-1:
            return num
        else:
            return 0
        
